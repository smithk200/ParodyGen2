I'm having trouble with a function in pokemon decompilation. In src/vs_seeker.c, I'm dealing with this line of code: if (HasTrainerBeenFought(sVsSeeker->trainerInfo[i].trainerIdx) != 1 || GetRematchTrainerIdFromTable(gRematchTable, sVsSeeker->trainerInfo[i].trainerIdx)) //refer to "sVsSeekerData" in the pokefirered version of this file { u32 trainer = sVsSeeker->trainerInfo[i].trainerIdx; DebugPrintf("Trainer: %d", trainer); //danny FlagClear(trainer); return sVsSeeker->trainerInfo[i].localId; } And, for reference, here's a line of code from include/opponents.h #define TRAINER_DANNY 414 The "trainer" u32 in the first function is set to 414, which is the number of the flag that TRAINER_DANNY is assigned to. For context, it's supposed to be a dynamic function, so I need the function to clear the appropriate flag name that corresponds with the opponent, not the specific flag " TRAINER_DANNY" every time.
Right now, it's literally clearing the number 414. How do I get this so that it clears the proper traienr flag?